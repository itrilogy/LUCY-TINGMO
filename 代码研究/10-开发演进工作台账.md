# 10 · 开发演进工作台账（完整清单）

> **文档性质**：后续增补开发的基础台账（Work Ledger）  
> **基准仓库**：Handy（Tauri 本地 ASR）  
> **工作分支语境**：相对上游 `main`（`ea3c20a`）的本地演进  
> **台账日期**：2026-07-29  
> **关联设计**：`00`–`09` 调研与方案文档  

---

## 0. 台账使用说明

| 项 | 说明 |
|----|------|
| 状态 | `done` 已完成 · `partial` 部分 · `todo` 待做 · `wont` 本期不做 |
| ID | 稳定编号，后续 PR/Issue 可引用 |
| 验收 | 可复现检查点 |
| 禁止 | 本台账不含任何密钥；`fixtures` 大音频默认 gitignore |

---

## 1. 产品目标与已拍板决策

### 1.1 目标

| ID | 目标 | 状态 |
|----|------|------|
| G1 | 现场录制 + 选定模型转写 | done |
| G2 | 可选 VAD 实时分段半实时转写 | done（默认关） |
| G3 | 长录音内容缩减/质量崩坏可修复 | done（Phase 0 + 77min 回归） |
| G4 | 结果不自动粘贴，扩大 overlay 展示与复制 | done |
| G5 | ASR 纠偏提示词 + 易错词（标识物）增量管理 | done |

### 1.2 产品决策（锁定）

| ID | 决策 | 状态 |
|----|------|------|
| D8 | **不自动粘贴**；仅用户点「复制」写剪贴板 | done |
| D9 | **VAD 半实时默认关**（`batch_auto_split` 默认） | done |
| D10 | 面板 = **扩大现有 overlay**，非独立会议窗 | done |
| D-M1 | 词库为用户级 `asr_markers.json`，不硬编码领域词 | done |

---

## 2. 分阶段交付总览

| 阶段 | 名称 | 状态 | 核心产出 |
|------|------|------|----------|
| Phase 0 | 长音频自动分片 | done | `audio_split` + `transcribe` 分段 |
| Phase 1 | 结果 overlay + 进度 + 禁粘贴 | done | `SessionResult/Progress` + UI |
| Phase 2 | VAD 半实时分段 | done | `SegmentPipeline` + 设置项 |
| Phase 3 | 标识物 + 纠偏范式落地 | done | `markers` + ASR Domain Correction |
| Phase 4+ | 台账中 `todo` 项 | partial | Session M1 + Obsidian 导出；见 §9 |

---

## 3. Phase 0 — 长音频自动分片

### 3.1 问题与证据

| ID | 内容 | 状态 |
|----|------|------|
| P0-ISSUE | SenseVoice 等模型 ~30s 窗；整段 17–77min 导致缩减/乱码 | done（已取证） |
| P0-EVID | 安装版日志 `beyond the ~30 s window`；history 30min→~500 字 | done |
| P0-GOLD | 黄金样例：鑫苑路9号 ≈77.3min；回归 160 段、18941 字、0 超窗 WARN | done |

### 3.2 代码变更清单

| ID | 路径 | 变更摘要 | 状态 |
|----|------|----------|------|
| P0-C1 | `src-tauri/src/audio_toolkit/audio_split.rs` | **新增** 分片规划（固定窗/能量切分）、join 文本 | done |
| P0-C2 | `src-tauri/src/audio_toolkit/mod.rs` | export 分片 API | done |
| P0-C3 | `src-tauri/src/managers/transcription.rs` | `transcribe` 按 `max_audio_ms`（默认 30s）循环 `transcribe_audio_slice` | done |
| P0-C4 | 同上 | 日志 `Auto-splitting … into N segment(s)`、`result_chars` | done |
| P0-C5 | 单元测试 `audio_split` | 覆盖短音频、固定窗、能量切、join | done |

### 3.3 验收

| ID | 验收项 | 状态 |
|----|--------|------|
| P0-A1 | `cargo test --lib audio_split` 通过 | done |
| P0-A2 | headless：`handy -f xinyuan-road-9-16k.wav --model SenseVoice…` 出现 Auto-splitting 160 段 | done |
| P0-A3 | 无 `beyond the ~30 s` WARN；字数数量级正确 | done |
| P0-A4 | 报告：`代码研究/fixtures/xinyuan-road-9-regression-report.md` | done |

### 3.4 后续增补（Phase 0 相关）

| ID | 项 | 状态 |
|----|-----|------|
| P0-T1 | 从模型 capability 读取 `max_audio_ms`（替代硬编码 30s） | done |
| P0-T2 | 分片进度 UI 与 batch 路径统一（长 batch 时已有 progress 事件部分） | done |
| P0-T3 | 段间 overlap + 去重拼接 | done（`DEFAULT_OVERLAP_MS=400` + `strip_boundary_overlap`） |

---

## 4. Phase 1 — 不自动粘贴 + 扩大 Overlay

### 4.1 代码变更清单

| ID | 路径 | 变更摘要 | 状态 |
|----|------|----------|------|
| P1-C1 | `managers/transcription.rs` | `SessionProgressEvent` / `SessionResultEvent` | done |
| P1-C2 | 同上 | 多段 batch 时 emit progress + cumulative StreamText | done |
| P1-C3 | `overlay.rs` | `result` 尺寸；`show_result_overlay` / `show_streaming_overlay_forced` | done |
| P1-C4 | `actions.rs` | 去掉自动 `paste`；展示 result + SessionResultEvent | done |
| P1-C5 | `commands/transcription.rs` | `copy_text_to_clipboard` / `dismiss_overlay` | done |
| P1-C6 | `lib.rs` | 注册 commands + events | done |
| P1-C7 | `RecordingOverlay.tsx/css` | result 面板：滚动全文 + 复制/关闭；分段进度文案 | done |
| P1-C8 | `bindings.ts` | 同步事件/命令类型 | done |
| P1-C9 | en/zh `translation.json` | overlay.copy / segmentProgress 等 | done |

### 4.2 验收

| ID | 验收项 | 状态 |
|----|--------|------|
| P1-A1 | 停录后文本进 overlay，不自动进焦点应用 | done（代码路径） |
| P1-A2 | 复制写入剪贴板；关闭 dismiss | done |
| P1-A3 | 编译通过 | done |

### 4.3 后续增补

| ID | 项 | 状态 |
|----|-----|------|
| P1-T1 | overlay 显式「粘贴到前台」按钮（非自动） | done |
| P1-T2 | 空结果/失败态更完整的文案 | done（`SessionResultStatus` empty/error + i18n） |
| P1-T3 | 清理 dead_code：`paste` 等 unused 警告 | done（legacy overlay helpers `allow(dead_code)`） |

---

## 5. Phase 2 — VAD 半实时分段

### 5.1 代码变更清单

| ID | 路径 | 变更摘要 | 状态 |
|----|------|----------|------|
| P2-C1 | `settings.rs` | `TranscriptionMode`：`BatchAutoSplit` / `VadSegmented` | done |
| P2-C2 | `managers/segment_pipeline.rs` | **新增** 段队列 + 串行 ASR worker | done |
| P2-C3 | `audio/recorder.rs` | 段缓冲、句尾 flush、30s 强制切；`SegmentCallback` | done |
| P2-C4 | `managers/audio.rs` | 挂载 pipeline；`segment_pipeline()` | done |
| P2-C5 | `actions.rs` | 模式分支：启动 pipeline / 禁 native stream / finalize | done |
| P2-C6 | `utils.rs` | cancel 时 pipeline.cancel | done |
| P2-C7 | `shortcut/mod.rs` | `change_transcription_mode_setting` | done |
| P2-C8 | `TranscriptionModeSelector.tsx` | 设置 UI | done |
| P2-C9 | AdvancedSettings + store + i18n | 接入 | done |

### 5.2 验收

| ID | 验收项 | 状态 |
|----|--------|------|
| P2-A1 | 默认模式仍为录完再转 | done |
| P2-A2 | 选 VAD 半实时：停顿后出段文本 | done（设计+代码） |
| P2-A3 | 空半实时结果回退 batch | done |
| P2-A4 | 编译通过 | done |

### 5.3 后续增补

| ID | 项 | 状态 |
|----|-----|------|
| P2-T1 | 半实时段间 initial_prompt 上下文 | done |
| P2-T2 | 中间段避免 `model_unload_immediately` 打断 | done |
| P2-T3 | 半实时进度 total 估计（当前 live total≈current） | done（`accepted` 原子计数 → total） |
| P2-T4 | 关闭 VAD 时半实时强制切窗的体验调优 | done（28s 软窗 + 能量切点 + 400ms overlap） |

---

## 6. Phase 3 — ASR 纠偏 + 标识物管理

### 6.1 代码变更清单

| ID | 路径 | 变更摘要 | 状态 |
|----|------|----------|------|
| P3-C1 | `markers.rs` | 词库模型、规则替换、glossary、解析【新增标识物】、merge | done |
| P3-C2 | `commands/markers.rs` | CRUD / accept pending / path | done |
| P3-C3 | `settings.rs` | `asr_markers_*` 三开关；内置 prompt `default_asr_domain_correct` | done |
| P3-C4 | `actions.rs` | 规则预替换 + glossary 注入 + 纠偏输出解析回写 | done |
| P3-C5 | `AsrMarkersSettings.tsx` | 词库 UI | done |
| P3-C6 | AdvancedSettings + store + i18n en/zh | 接入 | done |
| P3-C7 | `markers` 单元测试 4 项 | done |

### 6.2 运行时数据

| 路径 | 说明 |
|------|------|
| `~/Library/Application Support/com.pais.handy/asr_markers.json` | 用户级词库（不入库） |

### 6.3 验收

| ID | 验收项 | 状态 |
|----|--------|------|
| P3-A1 | `cargo test --lib markers` 4 passed | done |
| P3-A2 | 规则替换 longest-first | done |
| P3-A3 | 解析【新增标识物】→ pending | done |
| P3-A4 | Prompt 模板 + `${glossary}` | done |

### 6.4 后续增补

| ID | 项 | 状态 |
|----|-----|------|
| P3-T1 | Markdown 标识物清单导入工具 | done |
| P3-T2 | History UI「一词订正」→ 写入 markers | done |
| P3-T3 | 规则替换支持正则/忽略大小写选项 | done（`MarkerMatchMode` exact/ci/regex） |
| P3-T4 | 纠偏结果与 raw 对照 diff 视图 | done（overlay + history 对照切换） |

---

## 7. 调研与设计文档台账

| ID | 文件 | 内容 | 状态 |
|----|------|------|------|
| DOC-00 | `代码研究/00-调研总览.md` | 目标匹配与结论 | done |
| DOC-01 | `01-工程架构.md` | 架构与数据布局 | done |
| DOC-02 | `02-代码文件用途.md` | 文件职责表 | done |
| DOC-03 | `03-关键设计.md` | 引擎/VAD/流式 | done |
| DOC-04 | `04-数据流与管线.md` | 端到端数据流 | done |
| DOC-05 | `05-问题分析-长录音.md` | 根因分析 | done |
| DOC-06 | `06-安装版日志取证.md` | 本机日志/WAV/DB | done |
| DOC-07 | `07-与目标差距与改造建议.md` | 差距与改造 | done |
| DOC-08 | `08-问题需求与设计方案.md` | 需求+方案+决策 v1.x | done |
| DOC-09 | `09-ASR纠偏与易错词管理范式.md` | 通用范式 + 实现状态 | done |
| DOC-10 | `10-开发演进工作台账.md` | **本文件** | done |
| DOC-EV1 | `evidence-log-excerpts.txt` | 日志摘录 | done |
| DOC-EV2 | `evidence-runtime-data.txt` | WAV/history 快照 | done |
| DOC-FX | `fixtures/README.md` | 黄金音频说明（大文件 ignore） | done |
| DOC-REG | `fixtures/*-regression-report.md` | 77min 回归报告 | done |

### 7.1 fixtures 策略

| ID | 项 | 状态 |
|----|-----|------|
| FX-1 | `.gitignore` 忽略 `代码研究/fixtures/*` 大文件 | done |
| FX-2 | 保留 `fixtures/README.md` 与报告 md | done |
| FX-3 | 本地保留 m4a/wav 用于回归（不推送） | done |

---

## 8. 按模块汇总的「修改面」

### 8.1 后端 Rust（`src-tauri`）

| 模块 | 文件（新/改） | 阶段 |
|------|---------------|------|
| 分片 | `audio_toolkit/audio_split.rs` 新；`mod.rs` 改 | P0 |
| 转写 | `managers/transcription.rs` | P0/P1/P2 |
| 录音 | `audio/recorder.rs`；`managers/audio.rs` | P2 |
| 半实时 | `managers/segment_pipeline.rs` 新 | P2 |
| 动作 | `actions.rs` | P1/P2/P3 |
| Overlay | `overlay.rs` | P1 |
| 设置 | `settings.rs` | P2/P3 |
| 命令 | `commands/transcription.rs`；`commands/markers.rs` 新；`mod.rs` | P1/P3 |
| 词库 | `markers.rs` 新 | P3 |
| 快捷键设置 | `shortcut/mod.rs` | P2/P3 |
| 入口 | `lib.rs` | 全阶段 |
| 取消 | `utils.rs` | P1/P2 |

### 8.2 前端

| 模块 | 文件 | 阶段 |
|------|------|------|
| Overlay | `RecordingOverlay.tsx` / `.css` | P1/P2 |
| 设置 | `TranscriptionModeSelector.tsx` 新；`AsrMarkersSettings.tsx` 新；`AdvancedSettings.tsx` | P2/P3 |
| Store | `settingsStore.ts` | P2/P3 |
| 绑定 | `bindings.ts` | 全阶段 |
| i18n | `en` / `zh` translation.json | 全阶段 |

---

## 9. 建议后续开发 backlog（按优先级）

### P0 质量与能力

1. **P0-T1** 模型 `max_audio_ms` 能力驱动分片 — **done**（读 GGUF `Capabilities.max_audio_ms`，0→5min 分片，正数 clamp 5s–120s）  
2. **P1-T3** 清理 dead_code / unused paste 警告 — **done**（legacy compact overlay helpers allow；无 dead_code 告警）  
3. **P2-T2** 半实时中间段不触发 immediate unload — **done**（`TranscribeCallOptions.unload_after=false`，finalize 再 unload）  
4. **P0-T3** 段间 overlap + 去重拼接 — **done**（400ms leading overlap + char-level boundary dedup）  

### P1 体验

5. **P1-T1** overlay「粘贴到前台」显式按钮 — **done**（`paste_text_to_frontmost`）  
6. **P2-T1** 段间上下文 prompt — **done**（whisper initial_prompt 链式截断 200 字）  
7. **P3-T2** History 一词订正 — **done**（`correct_asr_word` + 历史条目 UI）  
8. **P1-T2** 空结果/失败态文案 — **done**（`SessionResultStatus::{Ok,Empty,Error}` + overlay i18n）  
9. **P2-T3** 半实时进度 total — **done**（worker 读 `accepted` 原子计数作为 total）  

### P2 词库与生态

10. **P3-T1** Markdown 标识物导入 — **done**（`import_asr_markers_markdown` + 设置页粘贴表）  
11. 领域清单离线转换脚本（可选，不入主仓硬编码） — **done**（`代码研究/scripts/md_markers_to_json.mjs`）  
12. 与外部 Obsidian 工作流的双向同步（可选） — todo  
13. **P3-T3** 规则替换正则/忽略大小写 — todo  
14. **P3-T4** 纠偏 raw/diff 视图 — todo  

### P3 架构

15. 轻量 `RecordingSession` 编排层（设计 M1）统一 batch / segment / stream — todo  
16. 可观测性：structured log 字段标准（session_id, segments, mode） — **done**（`session_id=… mode=… event=…` 键值日志）  
17. **P2-T4** VAD 强制切窗体验调优 — **done**（28s 软窗、尾部 2s 能量切点、400ms 段间 carry）  
18. **P3-T3** 规则替换正则/忽略大小写 — **done**（`MarkerMatchMode` + 设置 UI）  
19. **P3-T4** 纠偏 raw/diff 视图 — **done**（overlay `raw_text` 三态切换；history 对照）  

20. **RecordingSession 编排层（M1）** — **done（轻量）**（`session.rs` + `SessionHub`；mode/phase 状态机；actions/cancel 接入）  
21. **Obsidian 词库同步** — **done（显式导入/导出）**（export md / 写 `asr_markers.md` + `代码研究/scripts/obsidian-workflow.md`）  

### 仍开放

- 全量把 stop 转写体迁入 Session 方法（当前 Hub 管生命周期，ASR 仍在 actions）  
- Obsidian 文件监听热同步（明确不做 / 用外部 sync）  
- 人工手测执行：按 `11-手测清单与自检.md` 勾选（T3/T5）  

---

## 10. 测试与回归矩阵

| ID | 用例 | 期望 | 状态 |
|----|------|------|------|
| T1 | 短录音 2–10s batch | 正常文本 + overlay 复制 | done（逻辑）；GUI 见 `11` M1–M4 |
| T2 | 77min SenseVoice headless | Auto-split 160 段，~1.9 万字 | done |
| T3 | VAD 半实时开，说话停顿 | 分段追加 | **手测清单** `11` M14–M19 |
| T4 | 规则替换 verified 词对 | 误识被替换 | unit done；GUI 见 `11` M20/M23 |
| T5 | ASR Domain Correction + glossary | 注入词表；可写 pending | **手测清单** `11` M30–M33 |
| T6 | `cargo test --lib audio_split` | pass | done |
| T7 | `cargo test --lib markers` | pass | done |
| T7b | `cargo test --lib session` | pass | done |
| T8 | `cargo build --bin handy` | pass | done |
| T9 | 手测清单全文 | 见 `代码研究/11-手测清单与自检.md` | **ready**（待人执行勾选） |

---

## 11. 提交与仓库策略（台账记录）

| 项 | 说明 |
|----|------|
| **私有仓库** | https://github.com/itrilogy/handy-asr-lab （**PRIVATE**） |
| **origin** | `https://github.com/itrilogy/handy-asr-lab.git` |
| **upstream** | `https://github.com/cjpais/Handy.git`（开源上游，勿 force push） |
| **私有 main 提交** | `f640336` orphan 全量基线；**未含** `.github/workflows`（PAT 无 workflow 权限） |
| **本地 main 提交** | `8ace727` 基于上游历史的功能提交（含 workflows） |
| **本地分支** | `main`（开发）· `lab-main`（跟踪 origin/main） |
| 提交风格 | conventional commits：`feat:` / `fix:` / `docs:` / `chore:` |
| 密钥 | **禁止**提交 PAT；若在聊天中暴露过 **立即到 GitHub 撤销并轮换** |

### 11.1 同步提示

```bash
# 日常开发
git checkout main

# 推送到私有仓（需 token 具备 Contents 写；若含 workflows 还需 Workflows 写）
git push origin main   # 若被 workflow 权限拒绝，用 lab-main 快照策略

# 拉取上游
git fetch upstream && git merge upstream/main
```

---

## 12. Phase 品牌 — 听默 UI / 打包（2026-07-31）

| ID | 项 | 状态 | 说明 |
|----|-----|------|------|
| BR-DOC | 工程笔记与开放问题 | **done** | 见 **`12-听默品牌UI打包与开放问题归档.md`** |
| BR-UI | 名称/色/窗/Logo/i18n | done | 听默 + LUXI 色板；数据目录仍 `com.pais.handy` |
| BR-OV | Overlay 展开=结果尺寸、分段换行、空结果 | done | 详见文档 §2 |
| BR-PASTE | 粘贴到前台剪贴板竞态 | done | `paste_to_frontmost` |
| BR-UPD | 更新入口置灰 | done | 托盘 + 页脚 |
| BR-PKG | macOS dmg/ad-hoc 打包 | done | 见文档 §3；updater 私钥导致 exit 1 可忽略主体产物 |
| BR-AX | 辅助功能手动加 `.app` | done（手测） | `/Applications/听默.app` |
| BR-ID | 是否换 Bundle ID / 迁移 | **todo** | 开放问题 Q1 |
| BR-SIGN | 公证 / 自建更新 | **todo** | 开放问题 Q3–Q4 |

后续规划与 **Q1–Q10 开放问题** 一律写入 **`12`**，本台账只保留索引与状态。

---

## 13. 版本戳

| 字段 | 值 |
|------|-----|
| 台账版本 | v1.5 |
| 对应设计 | 08 v1.3 + M1 + **听默品牌（doc 12）** |
| 上游 tip | `ea3c20a`（记录时） |
| 本地 tip | SessionHub + 听默 UI/打包 |
| 私有仓 | `itrilogy/handy-asr-lab` |
| 维护 | 每完成一批 todo，更新 §9 / §12 与文档 `12` |

---

**使用方式**：新功能先在 §9 建 ID → 开发 → 改状态为 done → 必要时补 DOC/验收行。  
品牌/打包/数据身份类议题 → **`12` 文档**。  
本文件即为增补开发的**主台账入口**。
